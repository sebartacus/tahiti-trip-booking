import assert from "node:assert/strict";import test from "node:test";import{calculateSalonPayment,isSalonBaleinesDepositEligible,isSalonPecheDepositEligible}from"./salonPayment";
test("paiements Salon",()=>{assert.deepEqual(calculateSalonPayment(79000,"full"),{total:79000,paid:79000,balance:0,type:"full"});assert.deepEqual(calculateSalonPayment(79000,"deposit"),{total:79000,paid:23700,balance:55300,type:"deposit"});assert.deepEqual(calculateSalonPayment(110000,"deposit"),{total:110000,paid:33000,balance:77000,type:"deposit"});assert.deepEqual(calculateSalonPayment(290000,"deposit"),{total:290000,paid:87000,balance:203000,type:"deposit"})});
test("acomptes étendus Salon",()=>{assert.deepEqual(calculateSalonPayment(66000,"deposit"),{total:66000,paid:19800,balance:46200,type:"deposit"});assert.deepEqual(calculateSalonPayment(80000,"deposit"),{total:80000,paid:24000,balance:56000,type:"deposit"});assert.deepEqual(calculateSalonPayment(62500,"deposit"),{total:62500,paid:18750,balance:43750,type:"deposit"});assert.deepEqual(calculateSalonPayment(33500,"deposit"),{total:33500,paid:10050,balance:23450,type:"deposit"});assert.equal(isSalonPecheDepositEligible("two_plus_one"),true);assert.equal(isSalonPecheDepositEligible("place"),false);assert.equal(isSalonBaleinesDepositEligible(0),false);assert.equal(isSalonBaleinesDepositEligible(33500),true)});
